## Version v0.0.1 (Unreleased)

Initial Release

## 0.0.2

- Align with the live deployment: host networking (`network_mode = "host"`),
  Consul address via new `consul_addr` variable (default `127.0.0.1:8500`),
  default datacenter `kalmar`.
