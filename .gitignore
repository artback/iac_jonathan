.idea/
*.iws
*.iml
*.ipr
*iml

# Service vars may contain secrets (DB passwords etc.) — keep untracked,
# but allow examples through.
vars/*
!vars/*.example.hcl

# Real inventory may contain a Tailscale auth key.
ansible/inventory.ini

dashboard/
