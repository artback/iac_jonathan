# Playbooks live in playbooks/ while roles live in roles/, so the default
# lookup (<playbook_dir>/roles) misses them. Run ansible-playbook from this
# directory and both resolve.
[defaults]
roles_path = ./roles
inventory = ./inventory.ini
host_key_checking = False
# Silences the Python interpreter-discovery warning on the Pi.
interpreter_python = auto_silent
