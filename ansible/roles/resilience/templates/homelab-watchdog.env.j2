# Managed by ansible (roles/resilience). Do not edit by hand.
CONSUL_ADDR=http://127.0.0.1:8500
WATCHED_UNITS="{{ resilience_watched_units | join(' ') }}"
IGNORED_CHECK_SERVICES="{{ resilience_ignored_check_services | join(' ') }}"
PROBE_URL="{{ resilience_probe_url }}"
STATE_FILE=/var/lib/homelab-watchdog/state
BACKUP_DIR="{{ resilience_backup_dir }}"
BACKUP_MAX_AGE_HOURS={{ resilience_backup_max_age_hours }}
BACKUP_MIN_BYTES={{ resilience_backup_min_bytes }}
HEARTBEAT_URL="{{ resilience_heartbeat_url }}"
